# @package _global_

# to execute this experiment run:
# python train.py experiment=ddpm

defaults:
  - override /data: xla
  - override /model: xla_vgg16
  - override /callbacks: default
  - override /trainer: default

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["xla", "vgg16"]

seed: 12345

trainer:
  min_epochs: 10
  max_epochs: 54
  gradient_clip_val: 0.5

data:
  manifest: ${paths.data_dir}manifest.json
  batch_size: 512
  num_workers: 8

# ckpt_path: /home/minhlv/Projects/ComputerVision/SinoNom_recognition/logs/train/runs/2024-05-22_16-26-50/checkpoints/epoch_038.ckpt
ckpt_path: /home/minhlv/Projects/ComputerVision/SinoNom_recognition/logs/train/runs/2024-05-22_17-08-42/checkpoints/epoch_054.ckpt