# @package _global_

# to execute this experiment run:
# python train.py experiment=ddpm

defaults:
  - override /data: xla
  - override /model: xla_resnet
  - override /callbacks: default
  - override /trainer: default

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["xla", "resnet"]

seed: 12345

trainer:
  min_epochs: 2
  max_epochs: 5
  gradient_clip_val: 0.5

data:
  manifest: ${paths.data_dir}manifest.json
  batch_size: 512

logger:
  wandb:
    project: "SinoNom_recognition"
    name: "test_wandb"
    tags: ${tags}
    group: "test"
  aim:
    experiment: "test"

# ckpt_path: /home/ducpa/code/SinoNom_recognition/logs/train/runs/2024-05-21_12-45-36/checkpoints/epoch_004.ckpt